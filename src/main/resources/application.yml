server-updates:
  file:
    directory-path: /backup
    file-name: server-outage-backup.txt
  storage:
    max-size: 20

springdoc:
  swagger-ui:
    path: /api-docs.html

server:
  servlet:
    context-path: /serverOutageTracker/api/v1

spring:
  application:
    name: server-outage-tracker
